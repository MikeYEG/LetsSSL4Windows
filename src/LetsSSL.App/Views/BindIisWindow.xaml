<Window x:Class="LetsSSL.App.Views.BindIisWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bind certificate to IIS"
        Width="460" SizeToContent="Height"
        Background="{StaticResource Bg}"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Bind certificate to an IIS site" FontSize="16" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimary}" />
        <TextBlock Grid.Row="1" x:Name="IntroText" TextWrapping="Wrap" Margin="0,4,0,14"
                   Foreground="{StaticResource TextMuted}" />

        <StackPanel Grid.Row="2">
            <TextBlock Text="IIS site" FontWeight="SemiBold" Margin="0,0,0,4" />
            <ComboBox x:Name="SiteCombo" DisplayMemberPath="Name" Padding="8,6" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="OnCancel" />
            <Button Content="Bind" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="OnBind" />
        </StackPanel>
    </Grid>
</Window>
